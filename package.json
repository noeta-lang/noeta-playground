{
  "name": "noeta-playground",
  "version": "0.1.0",
  "private": true,
  "description": "The Noeta language playground — compile and run Noeta in the browser via WASM.",
  "type": "module",
  "packageManager": "pnpm@11.13.1",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.9.14",
    "@cloudflare/workers-types": "^4.20251011.0",
    "typescript": "^5.9.3",
    "vitest": "^3.2.4",
    "wrangler": "~4.44.0"
  }
}
